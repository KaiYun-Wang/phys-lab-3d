- 实验模块采用成对文件结构：每个实验均包含一个`*-page.tsx`（负责UI状态与控制逻辑）和一个`*-scene.tsx`（负责Three.js场景构建与物理帧循环）。
- 物理仿真循环中普遍使用`useRef`存储可变状态（如位置、速度），并通过`useFrame`直接修改引用以避免React重渲染开销，仅在需要更新UI时通过节流方式触发`useState`或回调。
- 所有实验页面均包裹在统一的`ExperimentContainer`组件中，该组件自动处理全屏Canvas适配、OrbitControls配置及全局光照环境。