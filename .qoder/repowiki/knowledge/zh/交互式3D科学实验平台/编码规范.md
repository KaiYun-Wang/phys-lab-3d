- 采用“-page.tsx”与“-scene.tsx”成对命名约定，前者负责React状态管理与UI控件，后者封装Three.js场景图与物理计算逻辑。
- 在3D场景组件中广泛使用`useRef`存储物理状态（如位置、速度），并通过`useFrame`直接修改引用以避免React重渲染开销，仅在数据上报时使用节流策略更新State。
- 所有实验入口页面均导出`dynamic = 'force-dynamic'`配置，确保服务端渲染时不缓存实验状态，保证客户端交互的实时性。